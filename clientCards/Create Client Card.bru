meta {
  name: Create Client Card
  type: http
  seq: 3
}

post {
  url: {{PATH}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "idClient":1,
    "idCard":10
  }
}
