meta {
  name: Create Card
  type: http
  seq: 3
}

post {
  url: {{PATH}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "isActive": "false"
  }
}

settings {
  encodeUrl: true
}
