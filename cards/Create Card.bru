meta {
  name: Create Card
  type: http
  seq: 3
}

post {
  url: {{PATH}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "expiration": 30,
    "maxStamp": 5,
    "description": "Esto es una descripcion nueva",
    "restrictions": "less than 3",
    "reward": "a thing",
    "name":"The best new new card name",
    "idBusiness":1
  }
}

settings {
  encodeUrl: true
}
